BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Course] ADD [gradingTerms] NVARCHAR(MAX);

-- AlterTable
ALTER TABLE [dbo].[Quiz] ADD [term] NVARCHAR(16);

-- AlterTable
ALTER TABLE [dbo].[Activity] ADD [term] NVARCHAR(16);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
