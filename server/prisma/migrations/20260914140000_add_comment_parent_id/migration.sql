BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ModuleComment] ADD [parentId] NVARCHAR(64);

-- AlterTable
ALTER TABLE [dbo].[AnnouncementReply] ADD [parentId] NVARCHAR(64);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
